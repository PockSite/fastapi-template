# --- Core ---
fastapi==0.128.0
uvicorn==0.40.0
python-dotenv==1.2.1

# --- Database ---
SQLAlchemy==2.0.46
psycopg2-binary==2.9.10   # driver Postgres/Supabase (no requerido para sqlite)

# --- Validación ---
pydantic==2.12.5
email-validator==2.3.0    # requerido por pydantic.EmailStr

# --- Seguridad (app/core/security.py) ---
python-jose==3.5.0        # JWT
passlib==1.7.4            # hashing de contraseñas
bcrypt==5.0.0
python-multipart==0.0.22  # formularios / OAuth2PasswordBearer

# --- Cliente HTTP (app/client/client.py) ---
httpx==0.28.1
